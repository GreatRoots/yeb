<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxxx.mapper.AdminMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xxxx.pojo.Admin">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="phone" property="phone" />
        <result column="telephone" property="telephone" />
        <result column="address" property="address" />
        <result column="enabled" property="enabled" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="userFace" property="userFace" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, phone, telephone, address, enabled, username, password, userFace, remark
    </sql>

    <update id="updateAdmin">
        UPDATE t_admin
        <set>
            <if test="name!=null||name!=''">
                `name`=#{name},
            </if>
            <if test="phone!=null||phone!=''">
                phone=#{phone},
            </if>
            <if test="telephone!=null||telephone!=''">
                telephone=#{telephone},
            </if>
            <if test="address!=null||address!=''">
                address=#{address},
            </if>
            <if test="enabled!=null||enabled!=''">
                enabled=#{enabled},
            </if>
            <if test="username!=null||username!=''">
                username=#{username},
            </if>
            <if test="password!=null||password!=''">
                password=#{password},
            </if>
            <if test="userFace!=null||userFace!=''">
                userFace=#{userFace},
            </if>
            <if test="remark!=null||remark!=''">
                remark=#{remark},
            </if>
        </set>
        WHERE id=#{id}
    </update>

    <select id="getAdminRolesById" resultMap="queryRole" parameterType="java.lang.Integer">
        SELECT
            r.id as rid,
            r.name as rname,
            r.nameZh as rnameZh
        from
            t_admin a,
            t_admin_role ar,
            t_role r
        WHERE
            a.id=ar.adminid
          and
            ar.rid = r.id
          and
            a.id = #{id}
    </select>

    <select id="queryAllAdmin" resultMap="queryRole">
        SELECT
            a.*,
            r.id rid,
            r.`name` rname,
            r.nameZh rnameZh
        FROM
            t_admin a,
            t_role r,
            t_admin_role ar
        WHERE
            a.id = ar.adminId
          AND ar.rid = r.id
          AND a.id != #{id}
        <if test="keywords!=null||keywords!=''"> AND a.`name`LIKE CONCAT('%','${keywords}','%')</if>
    </select>

    <resultMap id="queryRole" type="com.xxxx.pojo.Admin" extends="BaseResultMap">
        <collection property="roles" ofType="com.xxxx.pojo.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
            <result column="rnameZh" property="nameZh"/>
        </collection>
    </resultMap>

</mapper>
